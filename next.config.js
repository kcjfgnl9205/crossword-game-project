/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',
  // experimental: {
  // 	appDir: true
  // }
  // basePath: "/cross-word-project"
}

module.exports = nextConfig
